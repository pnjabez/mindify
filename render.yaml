services:
  - type: web
    name: mindify
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend.app:app"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

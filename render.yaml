services:
  - type: web
    name: mindify
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 1 --threads 2 --timeout 120 backend.app:app"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
